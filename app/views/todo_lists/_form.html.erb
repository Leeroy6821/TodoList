<%= form_with(model: todo_list, local: true) do |form| %>
      <% if todo_list.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(todo_list.errors.count, "error") %> prohibited this todo_list from being saved:</h2>

          <ul>
            <% todo_list.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <div class="form-group">
        <%= form.text_field :title, placeholder: :title, class: "form-control form-control-lg" %>
      </div>

      <div class="form-group">
        <%= form.text_area :description, rows: '5', placeholder: 'Enter your text ...', class: "form-control form-control-lg" %>
      </div>

      <div class="actions">
        <%= form.submit class: "btn btn-dark btn-lg mb-5" %>
      </div>
    <% end %>
  </div>
</div>
